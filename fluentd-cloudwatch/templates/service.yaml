apiVersion: v1
kind: Service
metadata:
  name: {{ include "fluentd-cloudwatch.fullname" . }}
  labels:
    {{- include "fluentd-cloudwatch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fluentd-cloudwatch.selectorLabels" . | nindent 4 }}
