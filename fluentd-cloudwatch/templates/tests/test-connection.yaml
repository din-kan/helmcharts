apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fluentd-cloudwatch.fullname" . }}-test-connection"
  labels:
{{ include "fluentd-cloudwatch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "fluentd-cloudwatch.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
